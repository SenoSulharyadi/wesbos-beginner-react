import React from 'react';

const NotFound = () => (
    <div>
        <h2>
            Page not found, <a href="/">back to the home page</a>
        </h2>
    </div>
);

export default NotFound;